<Window x:Class="WinCopies.GUI.Samples.AddNewCopyProcess"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinCopies.GUI.Samples"
        mc:Ignorable="d"
        Title="AddNewCopyProcess" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label>Source:</Label>
        <TextBox Grid.Column="1" Text="{Binding SourcePath}" />
        <Label Grid.Row="1">Destination:</Label>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding DestPath}" />
        <Button Grid.ColumnSpan="2" Grid.Row="2" VerticalAlignment="Bottom" Click="Button_Click">Ok</Button>
    </Grid>
</Window>
